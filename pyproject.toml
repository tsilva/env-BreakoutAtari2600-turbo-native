[build-system]
requires = ["maturin>=1.8,<2"]
build-backend = "maturin"

[project]
name = "env-breakoutatari2600-turbo-native"
version = "0.5.11"
description = "Deterministic, fixed-point, high-throughput Breakout vector environment"
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
license-files = ["LICENSE"]
authors = [
  { name = "Tiago Silva" },
]
keywords = [
  "atari",
  "breakout",
  "gymnasium",
  "reinforcement-learning",
  "vector-environment",
]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Intended Audience :: Science/Research",
  "Operating System :: MacOS :: MacOS X",
  "Operating System :: POSIX :: Linux",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Programming Language :: Python :: 3.14",
  "Programming Language :: Rust",
  "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
dependencies = [
  "gymnasium>=1.1,<2",
  "numpy>=1.26,<3",
]

[project.urls]
Changelog = "https://github.com/tsilva/env-BreakoutAtari2600-turbo-native/blob/main/CHANGELOG.md"
Discussions = "https://github.com/tsilva/env-BreakoutAtari2600-turbo-native/discussions"
Documentation = "https://github.com/tsilva/env-BreakoutAtari2600-turbo-native#readme"
Issues = "https://github.com/tsilva/env-BreakoutAtari2600-turbo-native/issues"
Repository = "https://github.com/tsilva/env-BreakoutAtari2600-turbo-native"

[project.scripts]
env-breakoutatari2600-turbo-native = "env_breakoutatari2600_turbo_native.cli:main"

[project.optional-dependencies]
play = [
  "pygame>=2.6,<3",
]
dev = [
  "maturin>=1.8,<2",
  "pytest>=9.0.3,<10",
  "ruff>=0.12,<1",
  "twine>=6.2,<7",
]

[tool.maturin]
module-name = "env_breakoutatari2600_turbo_native._env_breakoutatari2600_turbo_native"
python-source = "python"
features = ["pyo3/extension-module"]
include = [
  "python/env_breakoutatari2600_turbo_native/data/**/metadata.json",
  "python/env_breakoutatari2600_turbo_native/py.typed",
]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
target-version = "py311"

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I"]

[tool.uv]
exclude-newer = "7 days"
constraint-dependencies = [
  "mistralai!=2.4.6",
  "guardrails-ai!=0.10.1",
]
