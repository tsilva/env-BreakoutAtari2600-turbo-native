name: CI

on:
  push:
    branches:
      - main
  pull_request:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always
  UV_CACHE_DIR: .uv-cache

jobs:
  dependency-review:
    name: Dependency review
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false

      - name: Reject vulnerable dependency changes
        uses: actions/dependency-review-action@a1d282b36b6f3519aa1f3fc636f609c47dddb294 # v5.0.0
        with:
          fail-on-severity: moderate

  python:
    name: Python ${{ matrix.python-version }}
    runs-on: ubuntu-24.04
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12", "3.13", "3.14"]
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false

      - name: Set up Python
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6.3.0
        with:
          python-version: ${{ matrix.python-version }}

      - name: Set up uv
        uses: astral-sh/setup-uv@11f9893b081a58869d3b5fccaea48c9e9e46f990 # v8.3.2
        with:
          version: "0.11.13"

      - name: Install locked development environment
        run: uv sync --locked --extra dev --python "${{ matrix.python-version }}"

      - name: Build native extension
        run: .venv/bin/python -m maturin develop --release --locked

      - name: Run Python checks
        run: |
          .venv/bin/python -m ruff check .
          .venv/bin/python -m pytest -m "not stable_retro"

  minimum-dependencies:
    name: Minimum runtime dependencies
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false

      - name: Set up Python
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6.3.0
        with:
          python-version: "3.11"

      - name: Set up uv
        uses: astral-sh/setup-uv@11f9893b081a58869d3b5fccaea48c9e9e46f990 # v8.3.2
        with:
          version: "0.11.13"

      - name: Install locked test environment
        run: uv sync --locked --extra dev --python 3.11

      - name: Install minimum runtime dependencies
        run: >-
          uv pip install
          --python .venv/bin/python
          --upgrade
          --resolution lowest-direct
          --only-binary numpy
          "gymnasium>=1.1,<2"
          "numpy>=1.26,<3"

      - name: Build native extension
        run: .venv/bin/python -m maturin develop --release --locked

      - name: Show resolved runtime versions
        run: >-
          .venv/bin/python -c
          "from importlib.metadata import version;
          print('gymnasium=' + version('gymnasium'));
          print('numpy=' + version('numpy'))"

      - name: Run environment-contract and release tests
        run: >-
          .venv/bin/python -m pytest -q
          tests/test_env.py
          tests/test_release_build.py

  rust:
    name: Rust
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false

      - name: Check formatting
        run: cargo fmt --check

      - name: Check lints
        run: cargo clippy --locked --all-targets -- -D warnings

      - name: Run tests
        run: cargo test --locked --lib

  optional-extras:
    name: Optional player and trainer extras
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false

      - name: Set up Python
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6.3.0
        with:
          python-version: "3.13"

      - name: Set up uv
        uses: astral-sh/setup-uv@11f9893b081a58869d3b5fccaea48c9e9e46f990 # v8.3.2
        with:
          version: "0.11.13"

      - name: Install optional extras
        run: uv sync --locked --extra dev --extra play

      - name: Build native extension
        run: .venv/bin/python -m maturin develop --release --locked

      - name: Smoke optional imports and commands
        env:
          SDL_VIDEODRIVER: dummy
        run: |
          .venv/bin/python -c "import pygame; print(pygame.version.ver)"
          .venv/bin/python -m env_breakoutatari2600_turbo_native.play --help

  package:
    name: Package ${{ matrix.platform }}
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: macos-arm64
            runner: macos-15
          - platform: linux-x86_64
            runner: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false

      - name: Set up Python
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6.3.0
        with:
          python-version: "3.13"

      - name: Set up uv
        uses: astral-sh/setup-uv@11f9893b081a58869d3b5fccaea48c9e9e46f990 # v8.3.2
        with:
          version: "0.11.13"

      - name: Install locked build environment
        run: uv sync --locked --extra dev

      - name: Build supported wheel
        run: |
          .venv/bin/python .codex/skills/build-release/scripts/release_build.py \
            build-platform --platform "${{ matrix.platform }}"

      - name: Audit and smoke wheel
        shell: bash
        run: |
          set -euo pipefail
          shopt -s nullglob
          wheels=(wheelhouse-v*-${{ matrix.platform }}/*.whl)
          if (( ${#wheels[@]} != 1 )); then
            printf 'expected one wheel, found %s\n' "${#wheels[@]}" >&2
            exit 1
          fi
          .venv/bin/python .codex/skills/build-release/scripts/release_build.py \
            audit-wheels "${wheels[0]}"
          .venv/bin/python .codex/skills/build-release/scripts/release_build.py \
            smoke-wheel "${wheels[0]}"

  determinism-trace:
    name: Deterministic trace ${{ matrix.platform }}
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: macos-arm64
            runner: macos-15
          - platform: linux-x86_64
            runner: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false

      - name: Set up Python
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6.3.0
        with:
          python-version: "3.13"

      - name: Set up uv
        uses: astral-sh/setup-uv@11f9893b081a58869d3b5fccaea48c9e9e46f990 # v8.3.2
        with:
          version: "0.11.13"

      - name: Install locked development environment
        run: uv sync --locked --extra dev

      - name: Build native extension
        run: .venv/bin/python -m maturin develop --release --locked

      - name: Generate fresh public trace
        run: >-
          .venv/bin/python scripts/deterministic_trace.py generate
          --output deterministic-trace-${{ matrix.platform }}.json

      - name: Upload fresh public trace
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: deterministic-trace-${{ matrix.platform }}
          path: deterministic-trace-${{ matrix.platform }}.json
          if-no-files-found: error
          retention-days: 7

  determinism-compare:
    name: Compare supported-platform deterministic traces
    needs: determinism-trace
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false

      - name: Set up Python
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6.3.0
        with:
          python-version: "3.13"

      - name: Set up uv
        uses: astral-sh/setup-uv@11f9893b081a58869d3b5fccaea48c9e9e46f990 # v8.3.2
        with:
          version: "0.11.13"

      - name: Install locked environment
        run: uv sync --locked

      - name: Download fresh public traces
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          pattern: deterministic-trace-*
          path: traces
          merge-multiple: true

      - name: Compare fresh public traces
        run: >-
          .venv/bin/python scripts/deterministic_trace.py compare
          traces/deterministic-trace-macos-arm64.json
          traces/deterministic-trace-linux-x86_64.json
