# Use Stable Retro Turbo as the semantic oracle

The project uses the pinned Stable Retro Turbo release as its sole semantic oracle and compatibility target for canonical `Start` behavior. This deliberately accepts indirect, transient compatibility enforcement; the exact provider version is an operational pin that may change without changing this decision.
