interface:
  display_name: "Build Release"
  short_description: "Release through protected parity and PyPI gates"
  default_prompt: "Use $build-release to certify, publish, and verify an env-breakoutatari2600-turbo-native release."
